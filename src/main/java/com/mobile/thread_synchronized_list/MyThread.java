package com.mobile.thread_synchronized_list;

public class MyThread extends Thread {

    int index;

    MyThread(int index) {
        this.index = index;
    }

    @Override
    public void run() {
//        Main.myList.set(index, Main.myList.get(index) + 1);

//        for (int i = 0; i < Main.mySynchronizedList.size(); i++) {
//            Main.mySynchronizedList.set(i, Main.mySynchronizedList.get(i) + 1);
//        }

//        Main.mySynchronizedList.forEach(
//
//        );
    }
}
